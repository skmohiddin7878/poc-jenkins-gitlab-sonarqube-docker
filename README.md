# CI/CD POC – Jenkins + GitLab + SonarQube + Docker

## Flow
Developer → GitLab → Jenkins → SonarQube → Docker → User

## Access
Jenkins: http://<ip>:8080  
SonarQube: http://<ip>:9000  
App: http://<ip>:3000

